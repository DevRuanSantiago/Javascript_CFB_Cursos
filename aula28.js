let el = document.getElementsByTagName("div")
el = [...el]
el.map((e, i) => { e.innerHTML = "abacate" })

let elu = document.getElementsByTagName("div");
elu = [...elu];
elu.map((e, i) => {
    e.innerHTML = "cocobambu"
})